This is a test for the icon generator running in a docker container.

Run 'test-sils-in-container.sh' in its directory. I.e.:

./test-sils-in-container.sh

This assumes that the files in this directory are part of a full checkout
of SILS.